#!/usr/bin/python


import numpy as np
import pylab as py
import sys



fig = py.figure()


nfiles = len(sys.argv) -1 # number of files to plot

for i in range(nfiles):
  name = sys.argv[1+i] # name of the file
  m = np.genfromtxt(name).transpose()
  x = m[0]
  y = m[1]
#  if "-sorted" in sys.argv: # sort the plot
#    y = [iy for (ix,iy) in sorted(zip(x,y))]
#    x = sorted(x)
  #z = m[2]
  fig.set_facecolor("white")
  py.plot(x,y,linewidth=3.0,label=name,color="black",marker="o")
  py.xlim([min(x),max(x)])
#py.legend()
py.show()

